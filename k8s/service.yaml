apiVersion: v1
kind: Service
metadata:
  name: clinical-backend
  labels:
    app: clinical-backend
spec:
  type: ClusterIP
  selector:
    app: clinical-backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
